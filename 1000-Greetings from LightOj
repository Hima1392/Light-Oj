#include<stdio.h>
int main()
{
    int t;
    scanf("%d",&t);
    int fg=0;
    while(t--)
    {
        fg++;
        int a,b,sum;
        scanf("%d%d",&a,&b);
        sum=a+b;
        printf("Case %d: %d\n",fg,sum);

    }
    return 0;
}
