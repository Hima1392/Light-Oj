#include<stdio.h>
int main()
{
    int t;
    scanf("%d",&t);
    while(t--)
    {
        int n,d,f;
        scanf("%d",&n);
        d=n/2;
        f=n-d;
        printf("%d %d\n",d,f);
    }
    return 0;
}
